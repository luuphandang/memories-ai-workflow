# Integrations

Adapter layer reserved for optional external systems. Nothing under
`ai/integrations/` is a source of truth: canonical knowledge stays in `ai/shared/`,
`ai/repos/`, `ai/domains/`; task state stays in `ai/tasks/<ID>/`.
