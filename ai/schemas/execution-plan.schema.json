{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["task_id", "implementation_cycle", "change_cycle", "status", "slices"],
  "properties": {
    "task_id": {"type": "string"},
    "implementation_cycle": {"type": "integer", "minimum": 1},
    "change_cycle": {"type": "integer", "minimum": 0},
    "plan_version": {"type": "integer", "minimum": 1},
    "applied_patches": {"type": "array", "uniqueItems": true, "items": {"type": "string"}},
    "status": {"enum": ["draft", "ready", "in_progress", "completed"]},
    "slices": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "title", "depends_on", "acceptance_criteria", "requirement_details", "deliverables", "tests", "validation", "status"],
        "properties": {
          "id": {"type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
          "title": {"type": "string", "minLength": 1},
          "depends_on": {"type": "array", "uniqueItems": true, "items": {"type": "string"}},
          "acceptance_criteria": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
          "requirement_details": {"type": "object", "additionalProperties": {"type": "array", "uniqueItems": true, "items": {"type": "string"}}},
          "deliverables": {"type": "array", "minItems": 1, "items": {"type": "string"}},
          "tests": {"type": "array", "minItems": 1, "items": {"type": "string"}},
          "validation": {"type": "array", "minItems": 1, "items": {"type": "string"}},
          "status": {"enum": ["pending", "in_progress", "completed", "blocked"]},
          "external_dependencies": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["dependency_id", "capability", "blocking"],
              "properties": {
                "dependency_id": {"type": "string"},
                "capability": {"type": "string"},
                "required_version": {"type": ["integer", "null"]},
                "blocking": {"type": "boolean"}
              },
              "additionalProperties": false
            }
          },
          "resource_accesses": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["repository", "path", "access", "visibility"],
              "properties": {
                "repository": {"type": "string"},
                "path": {"type": "string"},
                "symbol": {"type": ["string", "null"]},
                "access": {"enum": ["READ", "WRITE"]},
                "visibility": {"enum": ["LOCAL", "TASK_SCOPED", "SHARED"]},
                "whole_file_writer": {"type": "boolean"}
              },
              "additionalProperties": false
            }
          },
          "evidence_requirements": {
            "type": "object",
            "description": "Optional, keyed by criterion string. Declares the minimum test level and real-infra dependencies build_evidence_matrix.py must see cited in that criterion's evidence before accepting it as passing.",
            "additionalProperties": {
              "type": "object",
              "required": ["min_test_level", "requires_real_infra"],
              "properties": {
                "min_test_level": {"enum": ["static", "unit", "integration", "contract", "e2e", "manual"]},
                "requires_real_infra": {
                  "type": "array",
                  "items": {"enum": ["database", "redis", "queue", "browser", "storage", "none"]}
                }
              },
              "additionalProperties": false
            }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
