# Example Feature — Workflow

## Trạng thái mẫu

```text
initial → processing → completed
                 └──→ failed
```

Các trạng thái, điều kiện chuyển và hành vi retry thực tế: `[BỔ SUNG THEO TÍNH NĂNG]`.
Cách biểu diễn trạng thái trong source: `[BỔ SUNG THEO DỰ ÁN]`.
